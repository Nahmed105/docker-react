language: generic 
sudo: required

services:
  - docker

before_install:
  - docker build -t nahmed105/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nahmed105/docker-react npm run test
